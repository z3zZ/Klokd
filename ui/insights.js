'use strict'

// Node-side insight engine. Populated in Phase 4.
// Returns [] until the insight functions are implemented.

function getInsights(dbPath, max = 2) {
  return []
}

module.exports = { getInsights }
